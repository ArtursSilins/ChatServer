<Window x:Class="ChatServer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChatServer.Data"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Closed="Window_Closed" Background="#FF6E6E6E">

    <Grid Margin="10 10 10 10" >
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>

        <TextBox FontSize="14" 
                 Text="{Binding ChatMessages}" 
                 VerticalScrollBarVisibility="Auto" >
            
        </TextBox>

        <Label Margin="10 10 10 10"
               Grid.Column="1"
               Content="{Binding UsersOnline}"
               FontSize="20"
               FontWeight="Bold"
               Foreground="White"
               HorizontalContentAlignment="Center">
            
        </Label>
    </Grid>
</Window>
